# Getting Started with DigitalOcean and Twingate

## Summary
Deploys a Twingate Connector on a DigitalOcean Droplet using `doctl` CLI and `cloud-init`. The Connector enables secure remote access to private DigitalOcean resources without exposing them publicly.

## Key Information
- Deployment method: `doctl` CLI + `cloud-init` script (command generated by Twingate Admin Console)
- One Droplet = one Connector with unique token pair
- Connector health verified via two status indicators: **Controller** and **Relay** (both must show `connected`)

## Prerequisites
- DigitalOcean account with API access
- `doctl` CLI installed and authenticated
- Twingate account with Admin Console access
- An existing Remote Network in Twingate

## Step-by-Step

1. **Generate tokens**: Admin Console → Remote Networks → select network → add/select Connector → See More → DigitalOcean → Step 2: Generate Tokens
2. **Copy install command**: Scroll to Step 4 in the Connector setup UI and copy the full `doctl` command
3. **Run command**: Paste into terminal — creates a Droplet with Connector pre-configured via `cloud-init`
4. **Verify in DigitalOcean**: Control Panel → Droplets → confirm Droplet status is running
5. **Verify in Twingate**: Admin Console → Remote Networks → select network → select Connector → confirm Controller and Relay show `connected`

## Configuration Values
- Tokens (Access + Refresh) are embedded in the generated `doctl` command automatically
- No manual env var configuration required — `cloud-init` handles Connector setup

## Gotchas
- **Never reuse token sets** — each Connector requires its own unique Access and Refresh token pair
- Copy/paste issues: terminal whitespace interpretation may corrupt the command; paste into a script file if needed
- `doctl` must be authenticated before running the generated command
- Verify Droplet creation independently: `doctl compute droplet list`

## Troubleshooting
| Issue | Fix |
|-------|-----|
| Token errors | Re-generate tokens; verify no copy/paste corruption |
| `doctl not found` | Install/authenticate `doctl` per DigitalOcean docs |
| Connector not connecting | Check Droplet is running; verify Connector service is active on Droplet |

## Related Docs
- Twingate Connector Management
- Twingate Resource Configuration
- Twingate Troubleshooting Docs
- DigitalOcean `doctl` installation guide