---
name: idfw-deployer
description: |
  Twingate Identity Firewall deployment specialist. Use this agent when the user
  needs to set up SSH PAM, configure the Twingate gateway for privileged access,
  implement session recording, or deploy K8s privileged access. Generates
  Terraform with twingate_gateway_config. Knows the SSH username lives in
  gateway YAML, not in the GraphQL API.
tools: Read, Grep, Glob, Bash, Write, Edit
skills: twingate-idfw, twingate-kubernetes, twingate-terraform
---

*Placeholder — full system prompt written in Session 5.*
